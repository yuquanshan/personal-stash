#!/usr/bin/python

from distutils.core import setup

setup(name='ada',
      version='1.0',
      description='Ada: the script that can answer questions regarding date',
      author='yuquanshan',
      packages = ['ada'],
      scripts=['bin/ada']
     )
