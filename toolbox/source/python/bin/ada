#!/usr/bin/python

import ada.ada
import pkg_resources
import sys

if 'show' in sys.argv or 'yourself' in sys.argv:
    fn = pkg_resources.resource_filename('ada', 'data/ada.dat')
    fi = open(fn, 'r')
    lines = fi.readlines()
    print(''.join(lines))
else:    
    ada.ada.main()
